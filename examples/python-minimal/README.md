# Minimal Python example

This is the original small FastMCP teaching example retained for comparison. It is **not** the production bridge, does not expose the 44-tool contract, and does not include the hardened path/network/process/service layers in `src/`.

Use the Node server documented in the repository README for real operation.
